{{{content}}}

<ul>
  {{#each root}}
  {{#if (not (eq @key "_self"))}}
  <hgroup>
    <h4><a href="/{{@key}}/">{{this.title}}</a></h4>
    <h2>{{this.date_published}}</h2>
  </hgroup>
  {{/if}}
  {{/each}}
</ul>